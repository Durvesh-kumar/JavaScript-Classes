/*
All JavaScript variable must be identified unique name (identifiers).

Names can contian letters [(a-z), (A-Z)], digites (0-9), underscor (_), dollerShing ($). We are not space ( ), (-)
Names must being with a latter.
Name not must begin with digites.
Name can also begin with $ and _.
Names are case sensitive (y and Y are different variables).
Reserved words (like JavaScript keywords) CANNOT be used as names.
*/


/*
CamelCase
Way of writing identifirs.

camelCase (JS naming cinvention) // let fullName

Snake_case (Python naming cinvention) // let full_name

PascalCase   // let FullName

*/
